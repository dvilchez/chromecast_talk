/** @jsx React.DOM */

'use strict';
var React = require('react');

var Shell = React.createClass({
    render: function() {
        return (
        	/*jshint ignore:start */

            <div>
            	<h2>Hello, World</h2>
            </div>
            /*jshint ignore:end */
        );
    }
});

module.exports = Shell;
