/** @jsx React.DOM */

'use strict';
var React = require('react');

var Video = React.createClass({
    render: function(){
        return(
            /*jshint ignore:start */
            <video src=""></video>
            /*jshint ignore:end */
            );
    }
});

module.exports = Video;
